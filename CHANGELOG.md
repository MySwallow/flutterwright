# 更新日志

## 未发布

### 新增
- 初始仓库脚手架（monorepo：skill + SDK + example + docs）。
- Skill `flutterwright`，包含 8 个 Playwright 风格的方法。
- SDK `flutter_visual_loop` 0.2.0，新增 `POST /reload` 端点。

设计动机参见 `docs/superpowers/specs/2026-05-22-flutterwright-design.md`。
